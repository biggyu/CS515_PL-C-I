args a b;
int x, y, z;
x = 1;
y = 2;
z = 3;

if a < b then {
    while x < 5 then {
        y = y + x * 2;
        x = x + 1;
    }
} else {
    while y < 10 then {
        z = z + y * (x + 1);
        y = y + 1;
    }
}

x = x + (z * (y + 2));
return x;
